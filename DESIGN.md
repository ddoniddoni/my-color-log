# DESIGN.md — 오늘빛 모바일 디자인 시스템

> 이 문서는 Google Stitch에서 만든 시안을 React Native 코드로 옮길 때의 공통 기준이다.  
> 제품: 오늘빛(가칭)  
> 플랫폼: iOS / Android 모바일 앱  
> 기준 화면: 390×844 portrait, 다양한 기기 크기에 적응

---

## 1. 브랜드 인상

오늘빛은 다음 감정을 줘야 한다.

- 조용한 발견
- 따뜻한 관찰
- 사적인 기록
- 가벼운 연결
- 시간이 쌓이는 만족감

다음 인상은 피한다.

- 어린이용 색깔 놀이
- 네온 게임 UI
- 랭킹 중심 경쟁 앱
- 공개 SNS 피드
- 과도하게 꾸민 감성 다이어리
- 카드와 배지가 빽빽한 습관 추적 앱

핵심 표현:

> 중립적인 다이어리 화면 위에 “오늘의 색” 한 가지가 살아 움직인다.

---

## 2. 시각 원칙

### 2.1 사진 우선

- 사용자의 사진이 화면에서 가장 강한 시각 요소다.
- 장식이 사진을 가리지 않는다.
- 사진 모자이크는 충분히 크고 여백이 안정적이어야 한다.

### 2.2 동적 포인트 색

- 앱 전체 기본 UI는 neutral palette를 사용한다.
- 오늘의 컬러를 `accent`로 주입한다.
- 화면 전체 배경을 매일의 강한 원색으로 채우지 않는다.
- accent는 hero, chip, progress, 선택 상태, 작은 하이라이트에 사용한다.

### 2.3 다이어리와 소셜의 균형

- 개인 화면은 편안하고 소장 가치가 느껴져야 한다.
- 친구방 화면은 2~6명의 멤버를 균형 있게 보여주되 승부표처럼 만들지 않는다.
- 멤버 진행도는 서로의 하루를 확인하는 정보이며 순위가 아니다.
- 2명일 때는 나란히, 3~6명일 때는 세로 카드 또는 2열 그리드로 자연스럽게 확장한다.

### 2.4 절제된 게임화

- 룰렛, 6장 완성, 9장 가득 채움에만 짧은 모션을 사용한다.
- 상시 반짝임, 코인, 점수 폭발, 과한 confetti를 사용하지 않는다.

---

## 3. 컬러 토큰

오늘의 컬러와 별개로 앱 베이스 컬러는 고정한다.

```ts
export const colors = {
  canvas: '#F7F5F1',
  surface: '#FFFFFF',
  surfaceMuted: '#F0EEE9',
  textPrimary: '#1D1C1A',
  textSecondary: '#6C6963',
  textTertiary: '#9A968E',
  border: '#E3E0D9',
  borderStrong: '#CBC7BE',
  overlay: 'rgba(20, 19, 18, 0.52)',
  success: '#2F7D59',
  warning: '#9A681B',
  danger: '#B74747',
  info: '#416E91',
  white: '#FFFFFF',
  black: '#000000'
};
```

### 3.1 동적 accent

매일 서버에서 다음 값을 받는다.

```ts
type DailyAccent = {
  accent: string;
  accentTint: string;
  accentShade: string;
  onAccent: string;
};
```

사용 범위:

- `accent`: 컬러 디스크, 선택, 진행 바, 주요 작은 포인트
- `accentTint`: 히어로 배경, 칩 배경, 달력 셀 배경
- `accentShade`: 텍스트 강조 또는 테두리
- `onAccent`: accent 위 텍스트/아이콘

주의:

- `accent`가 밝은 노랑처럼 대비가 낮을 수 있으므로 항상 서버의 `onAccent`를 사용한다.
- 상태 의미를 accent 하나로만 전달하지 않는다.

---

## 4. 타이포그래피

- 시스템 기본 산세리프를 우선한다.
- iOS는 San Francisco 계열, Android는 Roboto/Noto Sans KR 계열 자연스러운 fallback을 사용한다.
- 외부 폰트는 브랜드 확정 전 추가하지 않는다.

권장 스케일:

| Token | Size | Line height | Weight | 용도 |
|---|---:|---:|---:|---|
| display | 36 | 44 | 700 | 오늘의 컬러 이름, 핵심 공개 |
| title1 | 28 | 35 | 700 | 화면 제목 |
| title2 | 22 | 29 | 700 | 섹션 제목 |
| title3 | 18 | 24 | 600 | 카드/날짜 제목 |
| body | 16 | 24 | 400 | 기본 본문 |
| bodyStrong | 16 | 24 | 600 | 강조 본문/버튼 |
| callout | 14 | 20 | 500 | 보조 정보 |
| caption | 12 | 17 | 500 | 날짜/상태/메타데이터 |

규칙:

- 본문 16px 수준을 기본으로 한다.
- 카메라 핵심 컨트롤을 제외하고 12px 미만 텍스트를 사용하지 않는다.
- 컬러 이름은 한글과 영문을 함께 쓸 때 한글을 우선한다.
- 숫자 진행도는 읽기 쉽게 충분한 자간과 대비를 준다.

---

## 5. 간격

4pt 기반 스케일:

```ts
export const spacing = {
  0: 0,
  1: 4,
  2: 8,
  3: 12,
  4: 16,
  5: 20,
  6: 24,
  8: 32,
  10: 40,
  12: 48,
  16: 64
};
```

화면 기본:

- 좌우 padding: 20
- 작은 화면: 최소 16
- 섹션 사이: 32
- 카드 내부: 16~20
- 사진 모자이크 gap: 6~8
- 하단 CTA와 safe area 사이 충분한 여백

---

## 6. 모서리와 형태

```ts
export const radius = {
  sm: 8,
  md: 12,
  lg: 18,
  xl: 24,
  pill: 999
};
```

- 사진 썸네일: 12~16
- 일반 카드: 18
- 히어로 카드: 24
- 버튼: 16 또는 pill
- 컬러 디스크: 원형
- 카드 모서리를 모두 지나치게 둥글게 만들어 유아적으로 보이지 않게 한다.

---

## 7. 표면과 그림자

- 그림자는 최소화한다.
- 배경과 카드 구분은 border와 아주 약한 elevation으로 해결한다.
- Android/iOS에서 과도하게 다른 그림자가 생기지 않게 한다.

권장:

- 기본 카드: 1px border, 그림자 없음
- 떠 있는 카메라 CTA: 낮은 elevation
- 모달: overlay + 명확한 surface

글래스모피즘, 강한 blur, 두꺼운 drop shadow를 사용하지 않는다.

---

## 8. 아이콘

- 단순한 outline 아이콘
- 동일한 stroke 느낌 유지
- 카메라, 플래시, 추가, 공유, 설정, 달력, 친구 등 의미가 명확한 표준 아이콘 사용
- 장식용 아이콘을 남발하지 않는다.
- 아이콘 단독 버튼은 44×44 터치 영역과 접근성 레이블을 가진다.

---

## 9. 모션

### 9.1 룰렛

- 1.8~2.5초
- 빠르게 시작해 자연스럽게 감속
- 서버에서 정해진 색에 정확히 멈춤
- 멈춘 뒤 컬러 이름이 짧게 페이드/슬라이드 인
- 햅틱은 1회 가볍게

### 9.2 사진 추가

- 모자이크 새 셀에 180~240ms scale/fade
- 업로드 중 상태는 작은 비침투형 표시
- 실패 시 흔들림보다 명확한 배지와 재시도 사용

### 9.3 6장/9장

- 6장: 진행 링 또는 문구의 부드러운 완료 모션
- 9장: 1초 이내의 절제된 컬러 파동
- 전 화면 confetti 금지

### 9.4 접근성

- Reduce Motion에서 회전과 scale을 제거하고 페이드로 대체
- 의미 전달이 모션에만 의존하지 않음

---

## 10. 핵심 컴포넌트

### 10.1 `DailyMissionHero`

구성:

- 날짜/요일
- 오늘의 컬러 디스크
- 컬러 이름
- 짧은 관찰 문구
- 자정까지 남은 시간

상태:

- 공개 전
- 공개 중
- 공개 완료
- 서버 오류

### 10.2 `PhotoMosaic`

- 1~9장 적응형 레이아웃
- 사진 수가 적을 때 억지로 작은 3×3 셀에 가두지 않는다.
- 1장: 큰 카드
- 2장: 2분할
- 3장: 큰 1 + 작은 2 또는 균등 3
- 4장: 2×2
- 5~6장: 균형 그리드
- 7~9장: 3×3
- 빈 슬롯은 오늘 화면에서만 표시 가능
- 다이어리 상세에서는 실제 사진만 보여준다.

### 10.3 `ProgressSummary`

예시:

- `3 / 6` + “조금씩 오늘을 모으는 중”
- `6장 완성` + “오늘을 충분히 담았어요”
- `9장 가득` + “오늘의 색을 가득 채웠어요”

원형 게이지보다 단순 바 또는 텍스트와 셀 상태를 우선한다.

### 10.4 `CaptureButton`

- 주요 CTA
- 카메라 아이콘 + 라벨
- 오늘 빈 상태에서는 넓은 버튼
- 진행 중에는 floating 또는 모자이크 내 add tile
- 9장일 때 비활성/숨김

### 10.5 `RoomMemberProgressCard`

- 멤버 닉네임과 아바타/이니셜
- 사진 수와 0/6, 3/6, 6장 완성, 9장 가득 상태
- 최근 썸네일 최대 3개
- “새 사진 1장” 같은 상태
- 내 카드에는 촬영 CTA 또는 명확한 강조
- 승패 표현 없음

### 10.6 `RoomDailyBoard`

- 방 이름, 날짜, 공통 미션을 한 화면에서 연결한다.
- 2~6명의 멤버 카드를 동일한 위계로 배치한다.
- 2명은 세로 또는 2열, 3~6명은 세로 리스트나 반응형 2열을 사용한다.
- 각 카드에 아바타/이름, 사진 수, 최신 썸네일, 부드러운 상태 문구를 표시한다.
- 한 멤버가 비어 있어도 실패처럼 보이지 않는 빈 상태를 사용한다.
- 전체 멤버를 한 장의 소유자 없는 콜라주로 섞지 않는다.

### 10.7 `ColorCalendarDay`

- 날짜 숫자
- 해당 날짜 컬러의 작은 면 또는 점
- 기록 여부를 모양/채움으로 구분
- 선택, 오늘, 미래 상태
- 색 이름은 접근성 레이블에 포함

### 10.8 `SyncBadge`

상태:

- 동기화 중
- 오프라인 대기
- 업로드 실패
- 완료 상태는 상시 노출하지 않음

---

## 11. 화면 레이아웃

하단 탭은 `오늘 / 친구방 / 다이어리` 세 개를 사용한다.

### 11.1 오늘

위에서 아래 순서:

1. 상단 날짜 + 설정/프로필
2. DailyMissionHero
3. 진행 요약
4. PhotoMosaic
5. 촬영 CTA
6. 친구방 요약 카드
7. 선택적 일일 메모

사용자가 사진을 이미 찍었으면 사진을 hero 다음 핵심 영역으로 올려도 된다.

### 11.2 친구방

방 없음:

1. 화면 제목
2. 여러 개의 작은 개인 다이어리 카드가 하나의 컬러 미션으로 연결된 간결한 일러스트
3. 가치 문장
4. 친구방 만들기 CTA
5. 초대 코드 입력
6. “초대한 사람끼리만 보는 비공개 방이에요” 안심 문구

방 있음:

1. 방 이름, 멤버 수, 설정
2. 오늘의 공통 컬러 미션 + 날짜
3. 2~6명 멤버 진행 보드
4. 내 촬영 CTA
5. 날짜별 방 기록
6. 초대와 방 관리

### 11.3 다이어리

1. 월 전환 헤더
2. 월 기록 요약
3. 달력
4. 선택 날짜 미리보기
5. 날짜 상세 진입

월 전체가 색으로 채워지는 장면이 핵심 보상이다.

### 11.4 카메라

- 사진 앱처럼 기능을 과밀하게 넣지 않는다.
- 오늘의 컬러를 작은 칩 또는 얇은 가이드로 표시
- 셔터가 가장 큰 조작 요소
- 닫기/플래시 외 기능 최소화

---

## 12. 한국어 카피 샘플

### 온보딩

- “매일 하나의 색을 발견해요.”
- “스쳐 간 장면은 나만의 다이어리에 쌓여요.”
- “친구방을 만들면 같은 미션으로 서로 다른 하루가 보여요.”

### 오늘 빈 상태

- “오늘 스쳐 간 체리 레드를 찾아보세요.”
- “한 장만 남겨도 오늘의 기록이에요.”
- CTA: “첫 번째 색 발견하기”

### 진행 상태

- “파랑을 3장 모았어요.”
- “조금씩 오늘을 모으는 중”
- “6장의 초록으로 오늘을 완성했어요.”
- “9장의 노랑으로 오늘을 가득 채웠어요.”

### 오프라인

- “지금은 오프라인이에요. 사진은 안전하게 보관하고 있어요.”
- “연결되면 자동으로 다시 올릴게요.”

### 친구방

- “같은 미션, 서로 다른 하루.”
- “민지의 오늘에도 빨강이 하나 추가됐어요.”
- “우리 방에 세 가지 빨강이 모였어요.”
- “내 사진은 내 다이어리에 그대로 남아요.”
- “초대한 사람끼리만 보는 비공개 방이에요.”

---

## 13. 피해야 할 패턴

- 홈 화면을 9개의 빈 사각형만으로 시작
- 기록이 6장 미만이면 실패 처리
- 멤버 사진 수를 큰 점수판이나 순위표로 비교
- 레인보우 그라데이션을 상시 사용
- 모든 카드에 강한 그림자
- 작은 pill badge 남발
- 불필요한 통계와 streak를 첫 화면에 과밀 배치
- 다른 멤버 사진을 내 다이어리 그리드에 섞음
- 컬러 이름 없이 색 원만 표시
- 카메라 화면에 필터, 렌즈, 커뮤니티 기능을 동시에 노출

---

## 14. 반응형 및 기기 고려

- 기준 390×844이지만 320px대 폭에서도 핵심 CTA가 잘리지 않아야 한다.
- 큰 Android 화면에서 콘텐츠 폭이 과도하게 늘어나지 않도록 여백을 조절한다.
- landscape는 카메라를 제외하고 MVP 최적화 대상이 아니지만 깨지지 않아야 한다.
- bottom tab과 CTA는 safe area를 반영한다.
- 키보드가 캡션/닉네임 입력을 가리지 않는다.

---

## 15. Stitch → React Native 핸드오프 규칙

Stitch 결과를 넘길 때 다음을 함께 기록한다.

- 화면 이름
- 상태 이름
- 사용한 오늘의 accent 예시
- 컴포넌트 계층
- 상호작용
- 전환 대상 화면
- 오류/빈 상태
- 사용한 이미지 비율

Codex는 Stitch의 웹 코드를 그대로 붙이지 않고 다음 React Native 컴포넌트로 재구성한다.

| Stitch 개념 | React Native 구현 |
|---|---|
| page | Expo Router screen |
| section | View/feature component |
| CSS variable | `src/design/tokens.ts` |
| grid | Flexbox 또는 필요한 경우 RN grid 계산 |
| button | 공통 `Button` |
| card | 공통 `Surface` 또는 feature card |
| image | `expo-image` 또는 프로젝트 표준 Image |
| modal | React Native Modal/Router modal |

---

## 16. 디자인 수용 기준

- [ ] 오늘의 컬러가 분명하지만 UI 전체가 원색으로 과포화되지 않는다.
- [ ] 사진이 장식보다 우선한다.
- [ ] 솔로 화면이 완결되어 보인다.
- [ ] 친구방 화면이 경쟁 점수판처럼 보이지 않는다.
- [ ] 2명과 6명 상태가 모두 자연스럽게 확장된다.
- [ ] 각 멤버의 사진 소유권이 시각적으로 구분된다.
- [ ] 1장만 있는 날도 아름답게 보인다.
- [ ] 9장 모자이크가 안정적으로 보인다.
- [ ] 색 이름이 항상 텍스트로 제공된다.
- [ ] 작은 화면과 큰 글자에서 핵심 흐름을 사용할 수 있다.
- [ ] 로딩, 빈 상태, 오프라인, 업로드 실패 시안이 있다.
- [ ] iOS와 Android 모두 자연스러운 모바일 앱으로 보인다.

