# PRD — 컬러 다이어리 앱

> 프로젝트: **mycolorlog**  
> 문서 버전: 0.2  
> 상태: 친구방 중심 MVP 개발 기준안  
> 기준일: 2026-07-16  
> 대상 플랫폼: iOS / Android  
> 기술 방향: React Native + Expo + TypeScript + Supabase  
> 패키지 매니저: **npm only**  
> v0.2 핵심 변경: **지속되는 비공개 친구방을 만들고, 방 멤버가 매일 같은 미션을 수행하는 구조를 P0 핵심 기능으로 승격**

---

## 0. 한눈에 보는 제품 결정

이 문서에서 다음 사항은 MVP의 확정 기본값으로 취급한다.

1. 이 앱은 **개인 컬러 다이어리이면서, 소수의 친구와 매일 같은 미션을 수행하는 비공개 친구방 앱**이다.
2. 사용자는 방에 들어가지 않아도 매일 솔로 미션을 수행하고 개인 다이어리를 완전하게 사용할 수 있다.
3. 사용자는 이름이 있는 **지속형 친구방**을 만들거나 초대 코드/링크로 참여할 수 있다.
4. MVP에서 한 사용자는 동시에 최대 3개의 활성 친구방에 참여할 수 있다.
5. 한 친구방에는 방장을 포함해 최소 2명, 최대 6명의 활성 멤버가 참여할 수 있다.
6. 친구방은 하루가 끝나도 사라지지 않는다. 같은 멤버가 다음 날에도 새 미션을 함께 받는다.
7. 개인 기록은 사용자의 IANA 타임존, 친구방은 방 생성 시 고정한 IANA 타임존의 날짜를 기준으로 서버가 오늘의 미션을 확정하며, 같은 방의 모든 멤버에게 동일한 미션을 보여준다.
8. MVP의 미션 타입은 `color` 하나이며, 예시는 “오늘의 체리 레드를 찾아 1~9장 기록하기”다.
9. MVP에서는 솔로 사용자와 모든 친구방이 같은 전역 일일 컬러를 참조한다. 따라서 낮에 혼자 찍은 기록을 저녁에 방에 들어가도 같은 오늘 미션에 자연스럽게 연결할 수 있다.
10. 데이터 모델에는 `room_daily_missions`를 둬서 향후 방마다 다른 미션이나 커스텀 미션으로 확장할 수 있게 한다.
11. 사용자가 찍은 사진과 일일 기록의 원본 소유자는 항상 촬영한 사용자다.
12. 친구방은 개인 기록을 복제하지 않고 `entry_room_shares` 연결을 통해 멤버의 결과를 보여준다.
13. 같은 사진을 오늘 탭과 친구방 탭에서 다시 업로드하거나 중복 저장하지 않는다.
14. 친구방 참여 시 “이 방에서 내 당일 기록이 멤버에게 보인다”는 점을 명확히 안내하고 동의를 받는다.
15. 방을 나가거나 방이 종료되어도 내 사진과 개인 다이어리는 유지된다. 상대 기록에 대한 접근과 새 자동 공유만 중단된다.
16. 하루에는 1~9장의 사진을 남길 수 있다. 1장은 기록, 6장은 “오늘 완성”, 9장은 “가득 채움”이다.
17. 룰렛은 클라이언트 재추첨이 아니라 서버에서 이미 정해진 미션을 공개하는 연출이다.
18. MVP에서 사진 속 색상을 강제로 판정하지 않는다. 색상 일치도는 후속 비차단형 피드백이다.
19. MVP에는 공개 피드, 팔로워, 전체 랭킹, 댓글 스레드, 채팅을 넣지 않는다.
20. 사진은 네트워크가 없어도 로컬에 먼저 안전하게 저장되고, 연결 복구 후 업로드된다.
21. 사용자는 이메일과 비밀번호로 계정을 만든 뒤 솔로 기록을 시작하며, 같은 계정으로 모든 기기에서 자신의 데이터를 복구할 수 있다.

## 1. 제품 요약

### 1.1 한 문장 설명

> 매일 하나의 색으로 내 하루를 기록하고, 친구방을 만들면 같은 미션으로 모인 서로 다른 하루를 함께 보는 컬러 포토 다이어리.

### 1.2 핵심 경험

오늘의 미션이 “체리 레드 찾기”라면 사용자는 하루 동안 마주친 빨간 사물과 장면을 1~9장 촬영한다. 촬영한 사진은 먼저 자신의 개인 다이어리에 저장된다.

친구방 `퇴근길 색수집단`에 참여 중이라면 방 멤버 전원이 같은 체리 레드 미션을 받는다. 각 멤버는 자기 일상에서 발견한 빨강을 찍고, 친구방의 오늘 화면에서는 멤버별 진행도와 사진이 함께 보인다. 방에서 보이는 사진은 복사본이 아니라 각 사용자의 개인 기록을 권한에 따라 연결해서 보여주는 것이다.

다음 날이 되면 같은 방에 새로운 컬러 미션이 열린다. 방에는 날짜별 공동 기록이 쌓이고, 각 사용자의 다이어리에는 본인이 찍은 사진만 계속 남는다.

### 1.3 제품이 주는 가치

- 혼자 써도 “오늘 무엇을 기록하지?”라는 고민을 줄여주는 가벼운 일일 미션을 제공한다.
- 친구와 같은 미션을 받아도 서로 다른 장면이 모여 관계의 재미를 만든다.
- 긴 글이나 공개 게시 없이도 친구의 하루를 자연스럽게 확인할 수 있다.
- 기록의 원본이 개인 다이어리에 남아 친구방 참여 여부와 무관하게 소장 가치가 커진다.
- 방이 매일 유지되므로 일회성 초대가 아니라 작고 지속적인 공동 습관이 된다.
- 경쟁보다 관찰과 연결을 중심에 두어 장기 사용 피로를 낮춘다.

## 2. 문제 정의

기존 사진 다이어리는 사용자가 “무엇을 찍고 무엇을 기록할지” 스스로 결정해야 해서 진입 장벽이 있다. 반대로 챌린지 앱은 점수와 경쟁에 치우쳐 기록이 장기 자산으로 남지 않는 경우가 많다.

친구와 일상을 공유하는 기존 소셜 앱은 다음 문제를 가진다.

- 공개 범위가 넓어 게시 부담이 있다.
- 잘 꾸민 결과물만 올려야 한다는 압박이 있다.
- 친구의 하루를 자연스럽게 확인하기보다 피드를 소비하게 된다.
- 기록이 플랫폼의 게시물이 될 뿐 개인 다이어리로 정리되지 않는다.

이 앱은 “오늘의 색”이라는 매우 작은 관찰 과제를 제공하고, 결과를 개인 다이어리에 먼저 귀속시킨다. 친구방은 개인 기록 위에 얹는 지속형 공유 레이어로 작동하며, 같은 멤버가 매일 공통 미션을 이어간다.

---

## 3. 제품 비전

### 3.1 비전

사람들이 평범한 하루를 색으로 새롭게 발견하고, 혼자 또는 가까운 사람들과 그 발견을 오래 쌓아가게 만든다.

### 3.2 장기적 포지셔닝

- 감성 기록 앱과 데일리 미션의 중간
- 공개 소셜 네트워크가 아닌 소규모 비공개 친구방
- 점수 경쟁보다 같은 미션에서 나온 서로 다른 일상을 보는 서비스
- 시간이 쌓일수록 개인 다이어리와 방 기록의 가치가 동시에 커지는 제품

### 3.3 핵심 문장

> 혼자 기록해도 좋고, 친구방에서는 같은 미션으로 서로의 하루가 보이는 컬러 다이어리.

## 4. 제품 원칙

### P1. 개인 기록이 항상 원본이다

모든 사진은 촬영자의 개인 일일 기록에 먼저 저장된다. 친구방은 원본을 권한에 따라 참조한다.

### P2. 솔로 경험만으로 완결되어야 한다

친구방이 없거나 방 멤버가 당일 참여하지 않아도 앱이 비어 보이거나 실패한 느낌을 주지 않는다.

### P3. 친구방은 지속되는 공동 공간이다

방은 하루짜리 매칭이 아니다. 같은 멤버가 날짜가 바뀔 때마다 새 미션을 함께 받고 과거 방 기록을 다시 볼 수 있어야 한다.

### P4. 같은 방에는 같은 미션이 보장되어야 한다

미션은 서버에서 확정한다. 같은 `room_id + date_key`에 서로 다른 미션이 노출되어서는 안 된다.

### P5. 친구방은 경쟁보다 연결감을 강화한다

멤버의 진행도와 사진은 보여주되 순위, 승패, 뒤처짐을 전면에 두지 않는다.

### P6. 하루 한 장도 가치 있게 취급한다

9장을 채우지 못해도 실패로 표현하지 않는다. 1장은 기록, 6장은 완성, 9장은 가득 채움이다.

### P7. 오늘의 색이 주인공이고 앱 UI는 배경이다

중립적인 UI 위에 오늘의 미션 컬러를 동적 포인트로 사용한다.

### P8. 촬영 흐름은 매우 빨라야 한다

오늘 탭과 친구방 탭 어디에서 시작해도 동일한 개인 일일 기록에 빠르게 사진을 추가할 수 있어야 한다.

### P9. 사용자의 사진은 기본적으로 비공개다

공개 피드는 없다. 사용자가 가입한 친구방의 활성 멤버에게만 공유 연결된 기록을 보여준다.

### P10. 네트워크 상태가 기록을 방해하지 않아야 한다

촬영 파일은 로컬에 먼저 안전하게 보관하고 업로드 상태를 명확히 표시한다.

## 5. 목표와 비목표

### 5.1 MVP 목표

1. 사용자가 첫 실행 후 2분 안에 솔로 첫 사진을 남길 수 있다.
2. 친구 없이도 7일 연속 사용할 수 있는 개인 다이어리 경험을 제공한다.
3. 사용자가 이름이 있는 비공개 친구방을 만들고 최대 5명을 초대할 수 있다.
4. 같은 친구방의 2~6명이 매일 동일한 서버 미션을 받는다.
5. 오늘 탭 또는 친구방 탭에서 찍은 사진이 하나의 개인 기록으로 저장되고 방에는 참조로만 표시된다.
6. 방 멤버별 사진과 진행도를 같은 날짜 화면에서 확인할 수 있다.
7. 방에는 날짜별 공동 기록이, 개인 다이어리에는 본인의 날짜별 기록이 각각 쌓인다.
8. 방을 나가거나 방이 종료되어도 본인의 사진과 다이어리는 사라지지 않는다.
9. 앱 종료, 네트워크 실패, 재실행 이후에도 촬영 중인 기록이 유실되지 않는다.
10. 한 달간 기록이 쌓였을 때 컬러 달력과 날짜별 모자이크로 소장 가치를 느끼게 한다.

### 5.2 MVP 비목표

- 공개 피드
- 팔로우/팔로워 모델
- 불특정 다수 매칭
- 검색 가능한 공개 방
- 7명 이상의 대형 방
- 방장이 직접 만드는 커스텀 일일 미션
- 친구방별 서로 다른 컬러 스케줄
- 글로벌/지역 랭킹
- 실시간 채팅
- 긴 댓글 스레드
- 영상 업로드
- 사진 편집 필터
- AI 사물 인식
- 색상 판정 실패로 업로드 차단
- 유료 구독
- 광고
- 웹/PWA 버전
- 태블릿 전용 레이아웃 최적화

## 6. 대상 사용자

### 6.1 솔로 기록형

- 긴 글 대신 사진으로 일상을 남기고 싶다.
- 매일 기록하고 싶지만 소재를 정하는 것이 어렵다.
- 사진이 월별로 예쁘게 쌓이는 결과를 좋아한다.
- 친구 기능 없이도 앱을 쓴다.

### 6.2 친밀 공유형

- 연인, 절친, 가족과 서로의 하루를 가볍게 공유하고 싶다.
- 매일 대화하지 않아도 상대의 일상을 느끼고 싶다.
- 공개 SNS에는 올리기 부담스러운 소소한 사진을 공유하고 싶다.

### 6.3 가벼운 챌린지형

- 하루에 작은 미션이 있으면 행동하기 쉽다.
- 수집, 채우기, 스트릭 같은 가벼운 성취 요소를 좋아한다.
- 강한 경쟁보다 같이 완성하는 경험을 선호한다.

---

## 7. 용어 정의

| 용어 | 정의 |
|---|---|
| 오늘의 미션 | 사용자 또는 친구방에 적용되는 현지 날짜에 수행하는 일일 과제. MVP에서는 특정 컬러를 발견해 촬영하는 미션 |
| 전역 일일 미션 | 솔로 사용자와 모든 친구방이 MVP에서 공통으로 참조하는 날짜별 미션 |
| 방 일일 미션 | 특정 친구방과 날짜에 바인딩된 미션 레코드. MVP에서는 전역 일일 미션을 참조 |
| 컬러 공개 | 룰렛 애니메이션으로 이미 결정된 오늘의 컬러 미션을 보여주는 과정 |
| 개인 일일 기록 | 사용자 1명이 특정 날짜와 미션에 대해 만드는 개인 다이어리 원본 |
| 사진 기록 | 개인 일일 기록에 속한 1장의 사진 |
| 친구방 | 초대한 사람끼리 매일 같은 미션을 수행하는 지속형 비공개 공간 |
| 방 멤버 | 친구방에 가입한 사용자. MVP 활성 인원은 2~6명 |
| 방 제출 | 개인 일일 기록을 친구방의 해당 날짜 미션 화면에 보이도록 연결한 상태 |
| 공유 연결 | 개인 기록을 복제하지 않고 친구방에서 읽을 수 있게 하는 `entry_room_shares` 관계 |
| 기록됨 | 사진이 1장 이상 존재하는 상태 |
| 오늘 완성 | 사진이 6장 이상 존재하는 상태 |
| 가득 채움 | 사진이 9장 존재하는 상태 |
| 대기 업로드 | 로컬 촬영은 완료됐지만 서버 업로드가 끝나지 않은 사진 |

## 8. MVP 범위

### 8.1 P0 — 출시 차단 기능

#### 계정과 온보딩

- 최초 실행 시 짧은 3장 온보딩
- 이메일·비밀번호 회원가입 및 로그인
- 닉네임 설정
- 로그아웃, 계정 삭제

#### 오늘의 미션

- 날짜별 전역 일일 컬러 미션 조회
- 최초 1회 룰렛 공개 애니메이션
- 공개 후 같은 날에는 즉시 결과 표시
- 자정까지 남은 시간
- 날짜 변경 시 새 미션으로 전환
- 서버 조회 실패 시 캐시 또는 안전한 재시도 상태
- 클라이언트 임의 재추첨 금지

#### 촬영과 개인 일일 기록

- 앱 내 카메라 권한 요청
- 후면 카메라 촬영
- 촬영 미리보기와 재촬영
- 짧은 캡션 선택 입력
- 하루 최대 9장
- 사진 순서 변경과 삭제
- 1/6/9장 상태 표시
- 로컬 저장 후 비동기 업로드
- 업로드 실패 재시도
- 오늘 탭과 친구방 탭에서 같은 개인 기록 사용

#### 개인 다이어리

- 월 달력
- 날짜별 오늘의 컬러 표시
- 기록한 날짜 표시
- 날짜 선택 시 본인 사진 모자이크와 캡션 표시
- 사진 전체 보기
- 내 기록 수정/삭제
- 해당 기록이 어떤 친구방에 공유됐는지 보조 정보 표시

#### 친구방

- 활성 친구방이 없을 때 `방 만들기`와 `초대 코드 입력` 제공
- 방 이름과 선택적 이모지로 비공개 친구방 생성
- 방장 포함 활성 멤버 2~6명
- MVP 사용자당 활성 친구방 최대 3개
- 초대 코드와 공유 링크 생성
- 하나의 초대 링크로 남은 정원까지 여러 친구가 참여 가능
- 참여 전 방 이름, 방장, 현재 인원, 개인정보 안내 표시
- 방과 날짜별 `room_daily_missions` 생성
- 같은 방의 모든 멤버에게 같은 오늘의 미션 표시
- 멤버별 진행도와 사진 모자이크 표시
- 방에서 촬영하면 개인 기록에 저장되고 해당 방에 자동 공유 연결
- 오늘 이미 솔로로 찍은 사진이 있으면 참여 동의 후 같은 기록을 방에 연결
- 날짜별 방 기록 조회
- 방 나가기, 방장 위임 또는 방 종료
- 방을 나가도 내 다이어리는 유지

#### 개인정보와 안정성

- 비공개 사진 저장소
- 모든 사용자 데이터 테이블 RLS 적용
- 활성 방 멤버이며 공유 연결이 존재할 때만 다른 멤버 사진 접근
- 계정 삭제 시 DB와 Storage 정리
- 업로드 중 앱 종료 후 복구
- 네트워크 오류, 권한 거부, 초대 만료, 방 정원 초과 상태 처리

### 8.2 P1 — MVP 직후

- 아침/저녁 로컬 알림
- 방 멤버 사진에 한 번 탭 이모지 반응
- 완성 콜라주 이미지 저장/공유
- 비차단형 색상 일치도 점수
- 스트릭 및 월별 통계
- 방에 새 사진이 올라왔을 때 푸시 알림
- 방 초대 만료 시간 재설정
- 방장 권한으로 멤버 내보내기
- 친구 차단
- 다크 모드

### 8.3 P2 — 확장

- 방별 커스텀 미션
- 방별 서로 다른 일일 컬러
- 사진, 형태, 질감, 빛 등 컬러 외 미션 타입
- 여행 기간 한정 방
- 커플/가족용 방 테마
- 사용자별 목표 장수 설정
- 카카오 로그인
- 위젯
- 공개 전시형 피드
- AI 자동 색상 영역 탐지
- 컬러별 검색과 연간 회고

## 9. 정보 구조

### 9.1 하단 탭

1. **오늘**
   - 오늘의 미션
   - 내 진행 상태
   - 카메라 진입
   - 오늘 사진 모자이크
   - 활성 친구방 요약과 멤버 진행도 미리보기

2. **친구방**
   - 방 없음: 방 만들기, 초대 코드 입력
   - 방 있음: 방 이름, 오늘의 공통 미션, 멤버별 진행도와 사진
   - 방 날짜별 기록
   - 초대와 방 설정

3. **다이어리**
   - 월 달력
   - 날짜별 컬러 점
   - 개인 날짜 상세
   - 월별 요약

### 9.2 프로필과 설정

상단 아바타 또는 설정 아이콘에서 진입한다.

- 닉네임
- 계정 연결 상태
- 알림 설정
- 촬영 사진 기기 저장 여부
- 개인정보 처리 안내
- 활성 친구방 관리
- 로그아웃
- 계정 삭제

## 10. 핵심 사용자 흐름

### 10.1 첫 실행과 첫 솔로 기록

1. 앱 실행
2. 제품 가치 3장 온보딩
3. 이메일·비밀번호 회원가입 또는 로그인
4. 닉네임 입력
5. 오늘의 컬러 룰렛 공개
6. 오늘 화면 진입
7. “첫 번째 색을 발견하기” 탭
8. 카메라 권한 요청
9. 촬영
10. 미리보기에서 사용/재촬영 선택
11. 선택적 캡션 입력
12. 일일 기록 생성 후 사진 로컬 저장
13. 업로드 시작
14. 오늘 화면에서 1/6 상태 표시

### 10.2 하루 동안 여러 장 추가

1. 오늘 화면에서 `+` 버튼 탭
2. 촬영
3. 미리보기
4. 저장
5. 기존 모자이크에 즉시 추가
6. 업로드 완료 전에는 “동기화 중” 배지 표시
7. 6장 도달 시 작고 절제된 완성 모션
8. 9장 도달 시 “오늘을 가득 채웠어요” 표시

### 10.3 친구방 만들기

1. 친구방 탭 진입
2. `방 만들기` 탭
3. 이메일 인증 세션 확인
4. 방 이름 2~20자와 선택적 이모지 입력
5. 서버에서 방 생성, 생성자를 방장으로 등록
6. 초대 코드와 공유 링크 생성
7. 시스템 공유 시트로 친구에게 전송
8. 방은 정원 2~6명 범위에서 활성 상태를 유지
9. 친구가 들어오기 전에도 방 준비 화면과 오늘 미션을 확인 가능

### 10.4 친구방 참여

1. 딥링크 또는 6자리 초대 코드 입력
2. 이메일 인증 세션 확인
3. 방 이름, 방장 닉네임, 현재 인원, 최대 인원 표시
4. “참여하면 이 방의 멤버에게 내 당일 기록이 보여요” 안내
5. 사용자가 동의하고 참여
6. 서버 트랜잭션으로 정원과 중복 참여를 검증
7. 오늘 이미 개인 기록이 있으면 동일 미션인지 확인
8. 동일 미션이면 `entry_room_shares` 연결을 생성
9. 친구방 오늘 화면으로 이동

### 10.5 친구방에서 매일 같은 미션 수행

1. 방에 저장된 IANA 타임존의 날짜가 바뀌면 서버가 해당 방의 `room_daily_missions`를 준비
2. 모든 활성 멤버가 같은 컬러 이름과 안내 문구를 확인
3. 방 화면에 멤버별 `0/6`, `3/6`, `6장 완성`, `9장 가득` 상태 표시
4. 사용자가 방 화면의 카메라 CTA를 탭
5. 촬영 결과는 본인의 개인 일일 기록에 저장
6. 해당 개인 기록은 방에 참조로 공유
7. 다른 멤버 사진은 읽기만 가능
8. 사진이 추가되면 Realtime 또는 포그라운드 재조회로 갱신
9. 날짜가 지나면 해당 방 날짜 기록은 과거 기록으로 보존

### 10.6 개인 다이어리

1. 다이어리 탭 진입
2. 월 달력에서 각 날짜를 오늘의 컬러로 표시
3. 날짜 선택
4. 내가 찍은 사진만 표시
5. 방에서 촬영했더라도 개인 기록은 동일하게 내 다이어리에 남음
6. 친구 사진은 개인 다이어리 본문에 섞지 않음
7. “친구방에서 함께 본 이날” 링크로 별도 방 날짜 화면 이동 가능

### 10.7 친구방 나가기 또는 종료

1. 친구방 설정 진입
2. 일반 멤버는 `방 나가기`, 방장은 `방장 넘기기` 또는 `방 종료` 선택
3. 나가기 안내:
   - 내 개인 다이어리는 유지
   - 앞으로 새 기록은 방에 공유되지 않음
   - 다른 멤버 기록 접근은 종료
4. 방장이 다른 활성 멤버에게 권한을 넘기면 방은 유지
5. 마지막 멤버가 나가거나 방장이 명시적으로 종료하면 방 상태를 `ended`로 변경
6. 종료 후에도 각 사용자의 개인 기록은 유지

### 10.8 날짜 변경

1. 앱이 포그라운드로 돌아올 때 기기의 IANA 타임존과 개인 날짜 키를 확인하고, 친구방은 각 방의 타임존 날짜 키를 확인
2. 날짜가 바뀌었으면 새 전역 일일 미션 조회
3. 활성 친구방의 `room_daily_missions`를 조회하거나 서버에서 idempotent하게 생성
4. 전날 업로드 대기 사진은 전날 개인 기록으로 계속 업로드
5. 새 날짜의 룰렛 공개 상태는 별도로 저장
6. 오늘 탭과 친구방 탭은 동일한 새 미션을 표시
7. 전날 화면을 보고 있었다면 새 날 시작 안내 표시

## 11. 상세 기능 요구사항

### 11.1 계정 및 프로필

#### FR-AUTH-001 — 이메일·비밀번호 회원가입과 로그인

- 사용자는 첫 기록 전에 이메일 주소와 비밀번호로 회원가입하거나 로그인해야 한다.
- 회원가입 화면에서 생년월일로 만 14세 이상 여부를 확인하고, 만 14세 미만은 가입할 수 없다. 생년월일은 연령 확인에만 사용하며 계정·프로필·서버에 저장하지 않는다.
- 인증된 이메일 계정은 서버 사용자 ID와 개인 데이터 영역을 가진다.
- 사용자는 같은 이메일로 다시 로그인해 다른 기기에서도 자신의 기록을 복구할 수 있다.

#### FR-AUTH-002 — 이메일 계정 유지

- 친구방 생성, 초대 수락, 기기 간 복구를 포함한 모든 기능은 인증된 이메일 계정에서 사용한다.
- MVP에서는 익명 계정을 생성하거나 익명 계정 데이터를 별도 계정으로 이전하지 않는다.

#### FR-AUTH-003 — 닉네임

- 2~12자
- 앞뒤 공백 제거
- 금칙어 필터는 P1
- 친구방 화면에서 노출
- 전역 유일성은 요구하지 않음

#### FR-AUTH-004 — 계정 삭제

- 설정에서 직접 시작할 수 있어야 한다.
- 명확한 영향 범위를 안내한다.
- 관련 사진 파일과 개인 레코드를 제거한다.
- 다른 방 멤버의 개인 기록은 삭제하지 않는다.

### 11.2 오늘의 미션

#### FR-MISSION-001 — 서버 결정

- 오늘의 컬러 미션은 서버의 `daily_missions.challenge_date`로 결정한다.
- 클라이언트가 임의로 랜덤 색이나 미션을 확정해서는 안 된다.
- 같은 `date_key`에는 모든 솔로 사용자와 친구방이 동일한 전역 미션을 참조한다. 타임존이 다르면 같은 순간에도 서로 다른 날짜의 미션을 볼 수 있다.
- 같은 친구방과 날짜에는 하나의 `room_daily_missions.mission_id`만 존재해야 한다.

#### FR-MISSION-002 — 룰렛 공개

- 룰렛은 결과를 생성하지 않고 공개만 한다.
- 사용자별 날짜별 공개 여부를 로컬에 저장한다.
- 애니메이션 중 앱을 종료해도 다음 실행에서 동일 색을 보여준다.
- 애니메이션은 최대 약 2.5초를 넘지 않는다.
- 모션 감소 설정이 켜져 있으면 짧은 페이드로 대체한다.

#### FR-MISSION-003 — 컬러 미션 데이터

각 색은 최소 다음 속성을 가진다.

- 한국어 이름
- 영어 이름
- 대표 HEX
- 밝은 배경용 tint HEX
- 어두운 강조용 shade HEX
- 대비 텍스트 HEX
- 짧은 관찰 문구

예시:

```json
{
  "nameKo": "체리 레드",
  "nameEn": "Cherry Red",
  "hex": "#D94A4A",
  "tintHex": "#F9E6E6",
  "shadeHex": "#8D2727",
  "onColorHex": "#FFFFFF",
  "promptKo": "오늘 스쳐 간 빨강을 찾아보세요."
}
```

#### FR-MISSION-004 — 날짜 경계

- 개인 기록은 기기의 유효한 IANA 타임존을 프로필에 동기화하고 그 타임존의 자정을 사용한다.
- 친구방은 생성 시 방장의 IANA 타임존을 저장하며, 이후 모든 멤버에게 그 방 타임존의 자정을 사용한다.
- 타임존은 IANA 이름으로 저장하고 고정 UTC offset을 사용하지 않는다.
- DB에는 `date` 타입의 `challenge_date`를 저장한다.
- 사진의 촬영 시각은 UTC timestamp와 해당 개인/방 타임존으로 확정한 date key를 함께 유지한다.
- 타임존이 변경되어도 이미 저장된 기록의 date key는 다시 계산하거나 이동하지 않는다.

### 11.3 일일 기록

#### FR-ENTRY-001 — 유일성

- 사용자당 날짜별 일일 기록은 최대 1개다.
- DB 유니크 제약: `(user_id, challenge_date)`.

#### FR-ENTRY-002 — 생성 시점

- 첫 사진을 저장할 때 일일 기록을 생성한다.
- 사진이 없는 빈 기록을 미리 대량 생성하지 않는다.

#### FR-ENTRY-003 — 상태 계산

상태는 사진 수로 계산한다.

- 0장: `empty`
- 1~5장: `in_progress`
- 6~8장: `completed`
- 9장: `full`

상태를 DB에 저장할 경우 서버/트리거가 사진 수와 일치하도록 유지한다. 가능하면 파생 값으로 계산한다.

#### FR-ENTRY-004 — 일일 메모

- 날짜별 0~200자의 짧은 메모를 선택적으로 지원한다.
- 사진별 캡션과 별개다.

### 11.4 촬영 및 사진

#### FR-PHOTO-001 — 앱 카메라

- MVP 등록 경로는 앱 내 카메라다.
- 기본 후면 카메라
- 플래시 자동/켜기/끄기
- 핀치 줌은 선택 사항
- 갤러리 가져오기는 P1 이후 검토

#### FR-PHOTO-002 — 촬영 미리보기

- `다시 찍기`, `이 사진 사용` 제공
- 선택적 캡션 입력
- 오늘의 컬러 칩 표시
- 색상 판정 때문에 저장을 차단하지 않음

#### FR-PHOTO-003 — 개수 제한

- 날짜별 최대 9장
- 9장 상태에서 추가 버튼을 숨기거나 비활성화
- 삭제 후 다시 추가 가능

#### FR-PHOTO-004 — 정렬

- 기본 순서는 촬영 시각 오름차순
- 사용자가 길게 눌러 순서를 변경할 수 있다.
- `position`은 1~9의 정수이며 일일 기록 안에서 유일하다.

#### FR-PHOTO-005 — 파일 처리

- 원본을 그대로 업로드하지 않는다.
- EXIF 방향을 반영한다.
- 긴 변 기준 합리적인 크기로 축소한다.
- JPEG 또는 플랫폼 호환 포맷으로 압축한다.
- 얼굴/위치 EXIF를 서버 파일에 불필요하게 보존하지 않는다.
- base64를 DB에 저장하지 않는다.

#### FR-PHOTO-006 — 로컬 우선 저장

사진은 다음 상태를 거친다.

`local_saved → pending → uploading → synced`

실패 시:

`uploading → failed → pending → uploading`

- 촬영 직후 로컬 URI와 메타데이터를 영속 저장한다.
- 서버 업로드 전에도 오늘 화면에 표시한다.
- 앱 재실행 시 대기 큐를 복구한다.
- 사용자가 명시적으로 삭제하기 전에는 업로드 실패 때문에 로컬 파일을 제거하지 않는다.

#### FR-PHOTO-007 — 삭제

- 본인 사진만 삭제 가능
- 서버 파일, DB 레코드, 로컬 캐시를 정리
- 친구방 화면에서도 즉시 사라져야 함
- 업로드 대기 중인 사진도 삭제 가능

### 11.5 친구방

#### FR-ROOM-001 — 지속형 소규모 방

- MVP 친구방은 하루가 지나도 유지되는 비공개 공간이다.
- 활성 멤버는 방장 포함 최소 2명, 최대 6명이다.
- 친구가 아직 참여하지 않은 생성 직후에는 멤버 1명인 준비 상태를 허용한다.
- MVP에서는 사용자당 활성 친구방을 최대 3개 허용한다.
- DB와 UI는 여러 방을 명시적으로 다루기 위해 `room_id`를 사용한다.

#### FR-ROOM-002 — 방 생성과 관리

- 생성자는 방 이름 2~20자와 선택적 이모지를 설정한다.
- 생성자는 `owner`, 참여자는 `member` 역할을 가진다.
- 방 이름 변경, 초대 취소, 방장 위임, 방 종료는 권한 검증된 서버 작업으로 처리한다.
- 방장이 나가려면 다른 활성 멤버에게 방장을 넘기거나 방을 종료해야 한다.

#### FR-ROOM-003 — 다중 사용 초대

- 초대 토큰은 충분히 예측 불가능해야 하고 원문을 DB에 저장하지 않는다.
- 사용자에게는 6자리 표시 코드와 공유 링크를 제공한다.
- 기본 만료 시간은 24시간이다.
- 하나의 활성 초대는 남은 방 정원 범위에서 여러 친구가 사용할 수 있다.
- `max_uses`, `use_count`, 방 정원 검증을 서버 트랜잭션으로 처리한다.
- 본인 초대 수락, 같은 방 중복 참여, 활성 친구방 3개 보유, 만료·취소된 초대 참여를 차단한다.

#### FR-ROOM-004 — 방 일일 미션

- `room_daily_missions`는 `room_id + challenge_date`당 하나만 존재한다.
- 클라이언트가 미션을 결정하거나 랜덤 저장하지 않는다.
- MVP에서는 모든 방 일일 미션이 해당 날짜의 전역 `daily_missions`를 참조한다.
- 같은 방의 모든 활성 멤버는 동일한 미션 ID, 컬러, 문구를 받아야 한다.
- 데이터가 누락된 경우 권한 검증된 서버 함수가 idempotent하게 생성하거나 명확한 재시도 상태를 반환한다.

#### FR-ROOM-005 — 개인 기록과 방 제출

- 방에 보이는 결과는 개인 일일 기록의 복사본이 아니다.
- `entry_room_shares`가 개인 기록과 친구방을 연결한다.
- 사용자가 친구방에 참여할 때 당일 및 향후 기록 자동 공유에 대한 명시적 안내와 동의를 받는다.
- 방에서 카메라를 열어도 오늘 탭과 같은 개인 일일 기록을 사용한다.
- 참여 전 이미 만든 당일 기록은 미션이 같을 때 연결할 수 있다.
- 사용자 본인이 사진이나 기록을 삭제하면 방에서도 즉시 사라져야 한다.

#### FR-ROOM-006 — 멤버 보드

- 방 오늘 화면에 모든 활성 멤버의 닉네임, 사진 수, 상태, 최신 썸네일을 표시한다.
- 기본 정렬은 방장 우선이 아니라 `joined_at` 또는 안정된 멤버 순서다.
- 각 멤버 카드를 탭하면 그 멤버의 해당 날짜 공유 사진을 읽기 전용으로 본다.
- 승패, 순위, 점수 차를 기본 화면에서 강조하지 않는다.
- 0장인 멤버를 실패처럼 표현하지 않는다.

#### FR-ROOM-007 — 방 날짜 기록

- 친구방에는 날짜별 공통 미션과 당시 멤버 제출을 조회할 수 있는 기록 화면이 있다.
- 새 멤버는 가입일 이전의 다른 멤버 사진을 기본적으로 볼 수 없다.
- 개인 다이어리에는 본인 사진만 표시한다.
- 방 기록에서 개인 기록 상세로 이동할 수 있지만 다른 사람의 편집 기능은 노출하지 않는다.

#### FR-ROOM-008 — 실시간성과 복구

- 방 화면은 최초 서버 조회 후 멤버 사진과 진행도 갱신을 받을 수 있어야 한다.
- Realtime 구독이 실패해도 당겨서 새로고침, 포그라운드 재조회, 짧은 stale time으로 복구한다.
- 구독 채널은 현재 보고 있는 방과 오늘 날짜에 필요한 범위로 제한한다.

#### FR-ROOM-009 — 가시성과 종료

- 활성 방 멤버이며 `entry_room_shares`가 존재할 때만 다른 사용자의 기록과 사진을 읽을 수 있다.
- 방을 나간 사용자는 다른 멤버의 새 기록과 과거 기록에 대한 접근을 잃는다.
- 나간 사용자의 개인 다이어리와 본인 사진은 삭제하지 않는다.
- 방 종료 후 모든 멤버의 상호 접근은 종료하되 각자의 개인 기록은 유지한다.

#### FR-ROOM-010 — 진행도 표현

- `0/6`, `3/6`, `6장 완성`, `9장 가득`처럼 상태를 보여준다.
- 전체 방 완료율이나 멤버 순위는 MVP 핵심 화면에서 사용하지 않는다.
- 멤버가 참여하지 않은 날에도 비난, 독촉, 패배 문구를 사용하지 않는다.

### 11.6 다이어리

#### FR-DIARY-001 — 월 달력

- 현재 월 기본 표시
- 이전/다음 월 이동
- 날짜 셀에 해당 날짜의 컬러 표시
- 내 기록이 있는 날짜와 없는 날짜를 시각적으로 구분
- 오늘 날짜 강조

#### FR-DIARY-002 — 날짜 상세

- 날짜
- 컬러 이름과 색상
- 내 사진 모자이크
- 사진별 캡션
- 일일 메모
- 당시 공유한 친구방이 있으면 별도 링크

#### FR-DIARY-003 — 빈 날짜

- 기록이 없는 날짜도 당시 컬러를 볼 수 있다.
- “이날은 기록을 남기지 않았어요”처럼 중립적으로 표현한다.

#### FR-DIARY-004 — 미래 날짜

- 선택 불가 또는 비활성화
- 서버에 미래 날짜 기록 생성 금지

### 11.7 공유 및 내보내기

#### FR-SHARE-001 — 친구방 공유

- 개인 기록 공유는 사용자가 참여한 비공개 친구방 내부에 한정한다.
- 공개 URL로 원본 사진을 노출하지 않는다.

#### FR-SHARE-002 — 콜라주 내보내기(P1)

- 1~9장에 맞는 적응형 레이아웃
- 날짜, 컬러 이름, 앱 워터마크 선택
- 기기 저장 및 시스템 공유 시트
- 다른 방 멤버 사진을 포함한 합동 콜라주는 참여자 동의 정책을 별도로 정의하기 전까지 제공하지 않는다.

### 11.8 알림(P1)

- 오전: 오늘의 컬러가 열렸다는 알림
- 저녁: 아직 기록이 없거나 사용자가 설정한 목표에 미달한 경우만 알림
- 친구방 멤버가 새 사진을 추가했을 때 묶음 알림
- 알림 권한은 첫 실행 즉시 요구하지 않고 사용 가치가 전달된 뒤 요청
- 사용자가 시간과 종류를 끌 수 있음

---

## 12. 화면별 요구사항

### S01. 스플래시/세션 복구

- 브랜드 마크
- 세션, 로컬 업로드 큐, 날짜 키, 활성 친구방 목록 복구
- 2초 이상 걸리면 상태 메시지
- 치명적 오류 시 재시도

### S02. 온보딩

1. “매일 하나의 색을 발견해요”
2. “사진은 나만의 다이어리에 쌓여요”
3. “친구방을 만들면 같은 미션으로 서로 다른 하루가 보여요”

CTA: `오늘의 색 만나기`

### S03. 닉네임 설정

- 한 개 입력창
- 실시간 글자 수
- 다음 버튼

### S04. 컬러 룰렛 공개

- 중립 배경
- 중앙 룰렛 또는 컬러 디스크
- 서버 미션 결과로 부드럽게 수렴
- 결과 이름과 짧은 문구
- CTA: `오늘의 색 찾으러 가기`

### S05. 오늘 — 빈 상태

- 날짜와 남은 시간
- 오늘의 컬러 미션 히어로
- “오늘 스쳐 간 ○○을 찾아보세요”
- 큰 카메라 CTA
- “한 장만 남겨도 오늘의 기록이에요” 보조 문구
- 활성 친구방이 있으면 방별 이름과 현재 멤버 요약

### S06. 오늘 — 진행 중

- 적응형 사진 모자이크
- `3/6` 진행 문구
- 업로드 대기/실패 배지
- 추가 촬영 버튼
- 일일 메모
- 친구방 공유 상태: `퇴근길 색수집단에도 보여요`

### S07. 카메라

- 전체 화면 미리보기
- 상단: 닫기, 플래시
- 오늘의 미션 컬러 칩
- 선택적 프레임 가이드
- 하단 셔터
- 접근성 레이블

### S08. 촬영 검토

- 큰 사진 미리보기
- 재촬영
- 사용하기
- 캡션 입력
- 개인 다이어리 저장 안내
- 활성 친구방이 있으면 공유 대상 방 안내

### S09. 친구방 — 없음

- “같은 미션, 서로 다른 하루” 가치 설명
- CTA: `친구방 만들기`
- 보조 CTA: `초대 코드 입력`
- 공개 피드가 아닌 비공개 방이라는 안심 문구

### S10. 친구방 만들기

- 방 이름 2~20자
- 선택적 이모지 또는 작은 아이콘 선택
- 최대 6명 안내
- CTA: `방 만들기`
- 계정 미연결 상태에서는 계정 연결 선행

### S11. 친구 초대

- 방 이름과 현재 인원
- 6자리 코드
- 링크 복사
- 시스템 공유 시트
- 만료 시간과 남은 초대 가능 인원
- 초대 취소/새 코드 생성

### S12. 친구방 참여 확인

- 방 이름, 방장 닉네임, 현재 멤버 아바타
- 현재 인원/정원
- “참여하면 이 방에서 내 오늘 기록이 보여요” 안내
- CTA: `이 방에 참여하기`
- 활성 친구방 3개를 모두 사용 중일 때의 명확한 차단 상태

### S13. 친구방 — 오늘

- 방 이름과 멤버 수
- 오늘의 공통 컬러 미션
- 멤버별 진행 카드 또는 리스트
- 각 멤버의 사진 수와 최신 썸네일
- 내 촬영 CTA
- 2~6명에서 자연스럽게 동작하는 세로 스크롤 구조
- 승패와 랭킹 없음
- 과거 방 기록 진입

### S14. 친구방 — 멤버 날짜 상세

- 선택 멤버 닉네임
- 날짜와 공통 미션
- 공유된 사진 1~9장
- 읽기 전용
- 캡션 보기
- 다운로드, 수정, 삭제 기능 없음

### S15. 친구방 — 기록

- 날짜별 공통 컬러 목록
- 각 날짜 참여 인원과 썸네일 미리보기
- 날짜 선택 시 해당 날의 멤버별 기록
- 새 멤버에게 가입일 이전 기록을 노출하지 않는 상태

### S16. 다이어리 월

- 월 제목
- 달력
- 컬러 점/면
- 월 기록 일수
- 선택 날짜 미리보기

### S17. 다이어리 날짜 상세

- 날짜와 색
- 내 사진만 표시
- 일일 메모
- 편집
- 해당 날짜 친구방 기록 링크

### S18. 설정

- 계정 상태
- 닉네임
- 알림
- 사진 저장
- 활성 친구방 목록 관리
- 방장 위임/방 나가기/방 종료
- 개인정보
- 계정 삭제

### S19. 오류/오프라인

- 상단 비침투형 오프라인 배너
- 촬영은 계속 가능
- 업로드 실패 사진에 재시도 액션
- 초대 만료, 방 정원 초과, 활성 친구방 한도 도달 상태
- 전체 화면 차단은 서버 데이터가 반드시 필요할 때만 사용

## 13. UX 카피 원칙

### 13.1 말투

- 따뜻하지만 과하게 감성적이지 않다.
- 사용자를 평가하지 않는다.
- 짧고 구체적이다.
- 완료하지 못했다는 죄책감을 유발하지 않는다.

### 13.2 권장 문구

- “한 장만 남겨도 오늘의 기록이에요.”
- “오늘 스쳐 간 파랑을 찾아보세요.”
- “6장의 파랑으로 오늘을 완성했어요.”
- “조금 뒤 연결되면 자동으로 올릴게요.”
- “민지의 오늘에도 빨강이 하나 추가됐어요.”
- “같은 미션인데 모두 다른 하루가 모였어요.”
- “이 사진은 내 다이어리와 친구방에 함께 보여요.”

### 13.3 피해야 할 문구

- “실패했습니다.”
- “오늘 목표를 달성하지 못했어요.”
- “친구에게 졌어요.”
- “연속 기록이 깨졌습니다.”
- “사진이 부족합니다.”

---

## 14. 상태 모델

### 14.1 일일 기록 상태

```text
empty
  └─ 첫 사진 저장 → in_progress
in_progress
  ├─ 6장 도달 → completed
  └─ 모든 사진 삭제 → empty 또는 레코드 제거
completed
  ├─ 9장 도달 → full
  └─ 삭제로 5장 이하 → in_progress
full
  └─ 삭제로 8장 이하 → completed
```

### 14.2 사진 동기화 상태

```text
local_saved → pending → uploading → synced
                         └→ failed → pending
```

### 14.3 친구방 상태

```text
room: draft → active → ended
member: invited → active → left/removed
room day: scheduled → open → archived
invite: active → expired/revoked/exhausted
```

- 방 생성 직후 멤버가 한 명이어도 `draft` 또는 준비 상태를 허용한다.
- 첫 친구가 참여하면 `active`로 전환한다.
- 방 타임존의 날짜가 바뀌면 이전 방 날짜는 `archived`, 새 날짜는 `open` 상태가 된다.
- 방이 종료되어도 개인 일일 기록과 사진 상태는 변경하지 않는다.

## 15. 데이터 모델

아래는 논리 모델이다. 실제 SQL은 별도 migration으로 관리한다.

### 15.1 `profiles`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | `auth.users.id`와 동일 |
| nickname | text | 2~12자 |
| avatar_path | text nullable | 추후 사용 |
| timezone | text | 사용자의 유효한 IANA 타임존. 기존/미확인 값의 안전한 기본은 `Asia/Seoul` |
| is_onboarded | boolean | 온보딩 완료 |
| created_at | timestamptz | 생성 시각 |
| updated_at | timestamptz | 수정 시각 |

### 15.2 `color_palette`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | 컬러 ID |
| slug | text unique | `cherry-red` |
| name_ko | text | 체리 레드 |
| name_en | text | Cherry Red |
| hex | text | 대표색 |
| tint_hex | text | 밝은 배경색 |
| shade_hex | text | 어두운 강조색 |
| on_color_hex | text | 대비 텍스트색 |
| prompt_ko | text | 관찰 문구 |
| is_active | boolean | 팔레트 활성 여부 |

### 15.3 `daily_missions`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | 미션 ID |
| challenge_date | date unique | 타임존에서 확정된 전역 날짜 키 |
| mission_type | text | MVP `color` |
| color_id | uuid FK | 팔레트 참조 |
| title_ko | text | `오늘의 체리 레드` |
| prompt_ko | text | 촬영 안내 문구 |
| published_at | timestamptz | 공개 시각 |
| source | text | manual/scheduled/fallback |

제약:

- MVP에서는 날짜당 전역 일일 미션 하나
- `mission_type = 'color'`인 경우 `color_id` 필수

### 15.4 `daily_entries`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | 개인 기록 ID |
| user_id | uuid FK | 소유자 |
| mission_id | uuid FK | 당시 일일 미션 |
| challenge_date | date | 기록 생성 시 적용된 타임존의 날짜 키 |
| note | text nullable | 0~200자 |
| created_at | timestamptz | 생성 시각 |
| updated_at | timestamptz | 수정 시각 |

제약:

- unique `(user_id, mission_id)`
- unique `(user_id, challenge_date)` for MVP
- `challenge_date`는 연결된 미션 날짜와 같아야 함

### 15.5 `entry_photos`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | 사진 ID. 클라이언트 선생성 가능 |
| entry_id | uuid FK | 개인 일일 기록 |
| owner_id | uuid FK | RLS 단순화를 위한 소유자 |
| storage_path | text | private bucket 경로 |
| thumb_path | text nullable | 썸네일 |
| position | smallint | 1~9 |
| caption | text nullable | 0~80자 |
| captured_at | timestamptz | 실제 촬영 시각 |
| capture_date | date | 기록 생성 시 적용된 타임존의 촬영 날짜 키 |
| width | integer | 처리 후 폭 |
| height | integer | 처리 후 높이 |
| byte_size | integer | 파일 크기 |
| color_score | numeric nullable | P1 비차단 점수 |
| created_at | timestamptz | 생성 시각 |

제약:

- unique `(entry_id, position)`
- check `position between 1 and 9`
- `owner_id`는 연결된 `daily_entries.user_id`와 같아야 함
- 개인 일일 기록별 최대 9장

### 15.6 `rooms`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | 친구방 ID |
| name | text | 2~20자 |
| emoji | text nullable | 선택적 방 표시 |
| created_by | uuid FK | 생성자 |
| status | text | draft/active/ended |
| timezone | text | 생성 시 고정하는 유효한 IANA 타임존 |
| max_members | smallint | MVP 기본 6 |
| created_at | timestamptz | 생성 시각 |
| ended_at | timestamptz nullable | 종료 시각 |

제약:

- check `max_members between 2 and 6`
- 공개 검색 가능한 slug를 만들지 않는다.

### 15.7 `room_members`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| room_id | uuid FK | 친구방 |
| user_id | uuid FK | 사용자 |
| role | text | owner/member |
| status | text | active/left/removed |
| joined_at | timestamptz | 가입 시각 |
| left_at | timestamptz nullable | 종료 시각 |

제약:

- unique `(room_id, user_id)`
- 사용자당 활성 멤버십 최대 3개를 서버 함수와 트랜잭션으로 보장
- 방당 활성 멤버 수는 `rooms.max_members` 이하
- 활성 owner는 방당 정확히 한 명

### 15.8 `room_invites`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | 초대 ID |
| room_id | uuid FK | 대상 방 |
| inviter_id | uuid FK | 초대한 사용자 |
| token_hash | text | 원문 토큰 저장 금지 |
| display_code | text | 6자리 코드 |
| expires_at | timestamptz | 만료 |
| max_uses | smallint | 허용 사용 횟수 |
| use_count | smallint | 현재 사용 횟수 |
| revoked_at | timestamptz nullable | 취소 시각 |
| created_at | timestamptz | 생성 시각 |

제약:

- `use_count <= max_uses`
- 활성 코드의 충돌을 서버에서 방지
- 정원이 차면 남은 사용 횟수와 무관하게 수락 불가

### 15.9 `room_invite_uses`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| invite_id | uuid FK | 사용된 초대 |
| user_id | uuid FK | 참여 사용자 |
| accepted_at | timestamptz | 수락 시각 |

제약:

- unique `(invite_id, user_id)`
- 초대 수락과 `room_members` 생성은 같은 트랜잭션에서 처리

### 15.10 `room_daily_missions`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| room_id | uuid FK | 친구방 |
| challenge_date | date | 방 타임존 날짜 키 |
| mission_id | uuid FK | 전역 일일 미션 |
| created_at | timestamptz | 바인딩 시각 |

제약:

- unique `(room_id, challenge_date)`
- MVP에서 `mission_id.challenge_date = challenge_date`
- 같은 방과 날짜에 하나의 미션만 허용

### 15.11 `entry_room_shares`

| 컬럼 | 타입 | 설명 |
|---|---|---|
| entry_id | uuid FK | 개인 일일 기록 |
| room_id | uuid FK | 친구방 |
| shared_by | uuid FK | 기록 소유자 |
| shared_at | timestamptz | 공유 시작 |
| revoked_at | timestamptz nullable | 공유 취소 |

제약:

- unique `(entry_id, room_id)`
- `shared_by`는 개인 기록 소유자와 같아야 함
- 공유자는 공유 시점에 활성 방 멤버여야 함
- 개인 기록 미션은 해당 방 날짜의 `room_daily_missions.mission_id`와 같아야 함

### 15.12 `device_push_tokens` (P1)

- user_id
- expo_push_token
- platform
- device_id
- enabled
- updated_at

## 16. 사진 저장소 구조

버킷은 비공개로 유지한다.

```text
entry-photos/
  {user_id}/
    {challenge_date}/
      {photo_id}.jpg
      thumbs/
        {photo_id}.jpg
```

규칙:

- 앱에 Supabase service role key를 포함하지 않는다.
- 읽기는 권한 검증 후 signed URL 또는 인증된 다운로드를 사용한다.
- public URL을 데이터베이스에 저장하지 않는다.
- DB에는 `storage_path`만 저장한다.
- 파일 삭제와 DB 삭제가 불일치할 경우 정리 작업이 재시도 가능해야 한다.

---

## 17. 권한 및 RLS 원칙

### 17.1 기본 원칙

- 모든 앱 테이블에서 RLS 활성화
- 사용자는 자신의 프로필, 개인 기록, 사진만 수정 가능
- 다른 멤버는 방에 공유된 기록을 읽기만 가능
- 방 참여 여부와 공유 연결을 모두 확인
- 방을 나가거나 종료되면 다른 멤버 데이터 접근 제거
- 인증된 이메일 계정의 사용자 ID를 소유권과 접근 권한의 기준으로 사용

### 17.2 읽기 권한 개요

| 리소스 | 본인 | 같은 활성 친구방 멤버 | 비회원/다른 방 사용자 |
|---|---:|---:|---:|
| 내 프로필 전체 | 읽기/수정 | 제한 필드 읽기 | 불가 |
| 내 개인 일일 기록 | 읽기/수정 | 공유 연결 시 읽기 | 불가 |
| 내 사진 | 읽기/수정/삭제 | 공유 연결 시 읽기 | 불가 |
| 다른 멤버 기록 | 공유 연결 시 읽기 | 읽기만 | 불가 |
| 친구방 | 활성 멤버 읽기 | 활성 멤버 읽기 | 불가 |
| 방 일일 미션 | 활성 멤버 읽기 | 활성 멤버 읽기 | 불가 |
| 초대 | 생성/취소 권한자 | 유효 토큰 검증 후 참여 | 원문 목록 조회 불가 |

### 17.3 서버에서 강제할 불변식

다음은 UI 검사만으로 끝내지 않고 DB 제약, 트랜잭션 함수 또는 Edge Function으로 보장한다.

- 사용자당 활성 친구방 최대 3개
- 방 활성 멤버 최대 6명
- 방당 활성 owner 정확히 한 명
- 동시 초대 수락 시 정원 초과 방지
- 초대 만료, 취소, 사용 횟수, 자기 수락 검증
- `room_id + challenge_date`당 미션 하나
- 개인 기록 미션과 방 일일 미션 일치
- 본인 소유 기록만 방에 공유 가능
- 방장 위임과 방 종료의 원자성
- 계정 삭제와 Storage 정리
- 일별 미션 스케줄 생성

### 17.4 Storage

- 사진 버킷은 private이다.
- signed URL 또는 인증된 다운로드 전에 DB 권한을 확인한다.
- 경로를 추측해도 다른 방이나 다른 사용자의 사진을 읽을 수 없어야 한다.
- public URL을 영구 저장하지 않는다.

## 18. 클라이언트 아키텍처 요구사항

### 18.1 기술 스택

- React Native
- Expo
- TypeScript strict mode
- Expo Router
- Supabase
- TanStack Query: 서버 상태
- React Context 또는 작은 Zustand store: 촬영 드래프트와 UI 상태만
- React Native `StyleSheet` + 중앙 디자인 토큰
- React Hook Form + Zod: 폼이 복잡해지는 화면에 한해 사용

### 18.2 상태 구분

- 서버에서 온 데이터는 TanStack Query 캐시에 둔다.
- 서버 데이터를 Zustand에 복제하지 않는다.
- 촬영 대기 큐는 영속 로컬 저장소에 둔다.
- 인증 세션은 Supabase Auth와 SecureStore 기반 어댑터로 관리한다.

### 18.3 Repository 경계

UI 컴포넌트에서 Supabase SDK를 직접 호출하지 않는다.

예시:

```text
features/today/api/todayRepository.ts
features/entries/api/entryRepository.ts
features/rooms/api/roomRepository.ts
features/diary/api/diaryRepository.ts
```

---

## 19. 오프라인 및 동기화

### 19.1 요구사항

- 네트워크가 없어도 사진 촬영과 로컬 다이어리 표시 가능
- 서버 데이터가 없는 완전 최초 실행에서는 오늘의 전역 미션 조회가 필요하므로 친절한 재시도 상태 제공
- 이미 오늘의 전역 미션을 조회한 적이 있다면 캐시 사용
- 사진 업로드 큐는 앱 재시작 후 복구
- 중복 업로드를 막기 위해 클라이언트에서 UUID를 먼저 생성하고 idempotent upsert 사용

### 19.2 충돌 규칙

- 같은 사진 ID 재업로드: 기존 레코드 재사용
- 같은 날짜 일일 기록 동시 생성: unique 제약 후 기존 레코드 재조회
- 사진 순서 동시 변경: 마지막 서버 수정 승리, 단 본인만 수정 가능
- 삭제된 사진의 늦은 업로드 재시도: tombstone 또는 로컬 큐 취소 마커로 차단

---

## 20. 성능 요구사항

- 앱 콜드 스타트 후 캐시된 오늘 화면의 의미 있는 콘텐츠를 가능한 한 빠르게 표시
- 카메라 진입 체감 지연 최소화
- 오늘 화면에서 9장 썸네일을 원본 해상도로 로드하지 않음
- 월 달력 조회는 날짜 요약만 가져오고 사진 전체를 선조회하지 않음
- 친구방 실시간 구독은 현재 보고 있는 방과 오늘 날짜에 한정
- 이미지 업로드는 UI 스레드를 막지 않음
- 긴 목록은 가상화된 리스트 사용

권장 목표:

- 일반 네트워크에서 촬영 후 로컬 모자이크 반영: 300ms 내 체감
- 썸네일 파일: 가능하면 수백 KB 이하
- 오류 없는 세션 비율과 업로드 성공률을 주요 안정성 지표로 추적

---

## 21. 접근성

- 색만으로 상태를 구분하지 않는다.
- 오늘의 컬러에는 항상 색 이름 텍스트를 함께 표시한다.
- 텍스트 대비를 WCAG 기준에 맞게 검토한다.
- 최소 터치 영역 44×44pt 수준을 지향한다.
- 모든 아이콘 버튼에 접근성 레이블을 제공한다.
- Dynamic Type/글자 크기 확대에서 핵심 CTA가 잘리지 않게 한다.
- 모션 감소 설정 시 룰렛과 완료 모션을 축소한다.
- 사진에는 사용자 입력 캡션을 대체 텍스트로 활용할 수 있다.

---

## 22. 개인정보 및 안전

- 공개 피드 없음
- 연락처 전체 권한 요구 없음
- 초대는 링크/코드 기반
- 위치 정보 수집 없음
- 서버 사진에서 불필요한 EXIF 제거
- 친구방 나가기 또는 종료 시 다른 멤버 사진 접근 제거
- 스크린샷 방지는 MVP 범위 밖이며 완전한 방지로 약속하지 않음
- 개인정보 처리방침과 계정 삭제 경로 제공
- 미성년자 대상 서비스로 명시적으로 설계하지 않으며 추후 정책 검토

---

## 23. 분석 이벤트

분석 SDK는 어댑터 뒤에 두며 초기에는 no-op 구현이 가능하다. 사진 내용이나 캡션 원문을 분석 이벤트로 전송하지 않는다.

| 이벤트 | 주요 속성 |
|---|---|
| app_opened | session_type, date_key |
| onboarding_completed | auth_type |
| daily_mission_revealed | mission_type, color_slug, animation_skipped |
| camera_opened | source_screen, has_active_room |
| photo_captured | current_photo_count |
| photo_saved_local | photo_count |
| photo_upload_succeeded | retry_count, duration_bucket |
| photo_upload_failed | error_category |
| daily_entry_reached_1 | color_slug |
| daily_entry_reached_6 | color_slug |
| daily_entry_reached_9 | color_slug |
| room_create_started | account_state |
| room_created | room_size_limit |
| room_invite_created | remaining_capacity, channel |
| room_invite_accepted | member_count, has_existing_entry |
| room_daily_opened | member_count, participating_member_count |
| entry_shared_to_room | photo_count, shared_existing_entry |
| room_member_entry_opened | member_photo_count |
| room_history_opened | days_available |
| diary_day_opened | days_ago |
| account_linked | method |
| room_left | role, relationship_age_days |
| room_ended | member_count, room_age_days |

## 24. 성공 지표

### 24.1 활성화

- 온보딩 완료 → 첫 사진 저장 전환율
- 첫 실행 당일 첫 사진 저장까지의 중앙 시간
- 첫날 1장 이상 기록 비율

### 24.2 유지

- D1, D7, D30 기록 사용자 유지율
- 주간 기록 일수
- 한 달에 4일 이상 기록한 사용자 비율

### 24.3 핵심 행동

- 일일 평균 사진 장수
- 1장/6장/9장 도달 분포
- 다이어리 과거 날짜 재방문율
- 업로드 재시도 성공률

### 24.4 친구방

- 이메일·비밀번호 회원가입 완료율
- 방 만들기 시작 → 생성 완료율
- 초대 생성 → 첫 친구 참여 전환율
- 생성된 방의 평균 활성 멤버 수
- 활성 친구방의 주간 공동 참여 일수
- 하루에 2명 이상 기록한 방 비율
- 방 멤버 사진 조회 후 다음날 복귀율
- 방 날짜 기록 재방문율

### 24.5 가드레일

- 계정 삭제율
- 초대 만료/정원 초과 오류율
- 방 참여 직후 이탈률
- 사진 업로드 실패율
- 방 나가기·종료 후 불만 또는 재가입 실패 신호
- 권한 거부 후 이탈률

## 25. 주요 엣지 케이스

1. 자정 직전에 촬영하고 자정 이후 업로드됨
   - 촬영 당시 `capture_date`와 연결된 전날 개인 기록과 전날 방 미션에 저장한다.

2. 사용자가 기기 시간을 변경함
   - 프로필 및 참여 중인 방의 저장된 IANA 타임존을 기준으로 비정상 미래 기록을 차단한다.

3. 같은 초대 링크를 여러 친구가 동시에 수락함
   - 서버 트랜잭션에서 방 정원과 `use_count`를 잠그고 정원 이내 수락만 성공시킨다.

4. 활성 친구방 3개에 참여 중인 사용자가 새 방 초대를 수락함
   - MVP에서는 참여를 차단하고 현재 방 중 하나를 먼저 나가야 한다고 안내한다.

5. 방 정원이 6명인데 두 명이 마지막 한 자리를 동시에 수락함
   - 한 명만 성공하고 다른 사용자는 정원 초과 오류를 받는다.

6. 친구방 참여 전에 오늘 솔로 사진을 이미 찍음
   - MVP의 전역 미션이 같으므로 동의 후 기존 개인 기록을 방에 연결한다.

7. 방 일일 미션 레코드가 누락됨
   - 클라이언트 랜덤 생성 금지. 서버 함수가 해당 날짜 전역 미션을 참조해 idempotent 생성하거나 재시도를 반환한다.

8. 친구가 사진을 삭제함
   - 친구방 화면에서도 제거하고 해당 멤버 모자이크와 진행도를 갱신한다.

9. 사용자가 오프라인에서 9장을 찍음
   - 모두 로컬 큐에 유지하고 네트워크 복구 후 순차 업로드한다. 방 화면에는 본인 로컬 상태를 우선 표시한다.

10. 업로드 중 앱 제거
    - 이메일 계정으로 다시 로그인하면 서버 동기화 완료 데이터는 복구된다.

11. 카메라 권한 영구 거부
    - 설정 앱으로 이동하는 안내와 버튼을 제공한다.

12. 오늘의 미션 조회 실패
    - 캐시가 있으면 캐시 표시, 없으면 재시도 화면. 클라이언트 랜덤으로 확정하지 않는다.

13. 방장이 나가려고 함
    - 다른 활성 멤버에게 방장을 넘기거나 방을 종료해야 한다.

14. 일반 멤버가 방을 나감
    - 본인 다이어리는 유지되고 다른 멤버 기록 접근과 향후 자동 공유는 즉시 종료된다.

15. 방장이 방을 종료함
    - 상호 접근은 제거하되 각 사용자의 개인 기록과 사진은 삭제하지 않는다.

16. 새 멤버가 오래된 방에 참여함
    - 가입일 이전의 다른 멤버 공유 사진은 기본적으로 노출하지 않는다.

17. 사진 최대 9장 상태에서 방 화면의 카메라를 탭함
    - 추가 불가 상태를 명확히 표시하고 기존 사진 삭제 또는 교체만 허용한다.

## 26. MVP 수용 기준

### 계정

- [ ] 이메일·비밀번호 회원가입 또는 로그인 후 솔로 첫 사진을 남길 수 있다.
- [ ] 회원가입 화면에서 만 14세 미만은 가입할 수 없고, 생년월일은 서버에 저장하지 않는다.
- [ ] 이메일 인증 사용자에게 서버 사용자 ID가 생성된다.
- [ ] 같은 이메일로 로그인하면 기존 개인 데이터를 복구할 수 있다.
- [ ] 친구방 생성·참여 전에 이메일 인증 계정이 요구된다.
- [ ] 로그아웃과 계정 삭제가 동작한다.

### 오늘의 미션

- [ ] 같은 날짜에 두 기기가 같은 전역 컬러 미션을 표시한다.
- [ ] 같은 친구방의 모든 멤버가 동일한 `room_daily_mission`을 본다.
- [ ] 앱 재실행 시 룰렛 결과가 바뀌지 않는다.
- [ ] 개인/방 각각의 타임존 자정 후 새 미션으로 전환된다.
- [ ] 모션 감소 설정에서 대체 애니메이션을 사용한다.

### 사진 기록

- [ ] 카메라 촬영, 재촬영, 저장이 가능하다.
- [ ] 1~9장을 추가할 수 있고 10번째는 차단된다.
- [ ] 1/6/9 상태가 정확히 표시된다.
- [ ] 오늘 탭과 친구방 탭에서 촬영해도 같은 개인 일일 기록에 저장된다.
- [ ] 업로드 실패 후 앱 재실행과 네트워크 복구 시 재시도된다.
- [ ] 사진을 삭제하면 친구방 화면에서도 제거된다.

### 다이어리

- [ ] 월 달력에서 날짜별 컬러와 기록 여부를 볼 수 있다.
- [ ] 날짜 상세에는 본인 사진만 보인다.
- [ ] 방에서 촬영한 사진도 본인 다이어리에 남는다.
- [ ] 과거 기록을 수정/삭제할 수 있다.

### 친구방

- [ ] 방 이름을 지정해 친구방을 만들 수 있다.
- [ ] 초대 코드/링크로 여러 친구가 방 정원까지 참여할 수 있다.
- [ ] 활성 멤버 수가 6명을 넘지 않는다.
- [ ] 한 사용자가 동시에 속한 활성 방이 3개를 넘지 않는다.
- [ ] 방 멤버 모두가 날짜별로 같은 미션을 받는다.
- [ ] 멤버별 진행도와 공유 사진을 볼 수 있다.
- [ ] 방에는 날짜별 기록이 쌓인다.
- [ ] 친구 사진은 내 개인 다이어리에 복제되지 않는다.
- [ ] 방을 나가거나 종료해도 내 다이어리는 유지된다.
- [ ] 방을 나간 뒤 다른 멤버 사진 접근이 제거된다.

### 보안

- [ ] 모든 사용자 데이터 테이블에 RLS가 켜져 있다.
- [ ] 다른 방 사용자의 사진 경로로 접근할 수 없다.
- [ ] 초대 수락의 정원/사용 횟수 검증이 원자적이다.
- [ ] 앱 번들에 service role key가 없다.
- [ ] 사진 버킷이 public이 아니다.

### 품질

- [ ] TypeScript typecheck 통과
- [ ] lint 통과
- [ ] 핵심 도메인 로직 단위 테스트 통과
- [ ] iOS와 Android 실제 기기에서 촬영 플로우 확인
- [ ] 2명과 6명 친구방 레이아웃 확인
- [ ] 작은 화면과 큰 글자 설정에서 핵심 CTA 사용 가능

## 27. 개발 단계

### Phase 0 — 프로젝트 기반

- Expo + TypeScript + Expo Router 생성
- npm 및 `package-lock.json` 고정
- 디자인 토큰과 공통 컴포넌트
- Supabase 클라이언트
- 환경 변수 검증
- QueryClient
- 오류 경계와 로깅 어댑터

### Phase 1 — 완전한 솔로 수직 슬라이스

- 이메일·비밀번호 세션
- 닉네임 온보딩
- 오늘의 전역 미션
- 룰렛 공개
- 카메라
- 로컬 사진 큐
- 개인 일일 기록 업로드
- 오늘 모자이크
- 월 다이어리

### Phase 2 — 친구방

- 이메일·비밀번호 계정 연결
- 방 생성과 이름 설정
- 멤버 2~6명 구조
- 다중 사용 초대 코드/링크
- 방 일일 미션 바인딩
- 개인 기록 공유 연결
- 멤버 보드와 멤버 날짜 상세
- 방 날짜별 기록
- 실시간 또는 포그라운드 갱신
- 방장 위임, 나가기, 종료

### Phase 3 — 안정화

- RLS 침투 테스트
- 동시 초대 수락과 정원 테스트
- 업로드 재시도와 충돌 처리
- 계정 삭제
- 접근성
- 빈 상태/오류 상태
- 2명/6명 실제 기기 QA

### Phase 4 — P1

- 알림
- 반응
- 콜라주 공유
- 비차단 색상 점수
- 스트릭/통계

## 28. 구현 우선순위 원칙

기능 우선순위가 충돌하면 다음 순서로 판단한다.

1. 사진 유실 방지
2. 개인 다이어리 소유권 보장
3. 권한/보안
4. 촬영 속도
5. 솔로 사용성
6. 친구 연결성
7. 시각적 완성도
8. 게임화와 부가 기능

---

## 29. 확정된 제품 트레이드오프

### 29.1 9장을 강제하지 않는다

다이어리로서의 지속성을 위해 한 장도 유효한 기록으로 인정한다. 6장과 9장은 성취 단계일 뿐 저장 조건이 아니다.

### 29.2 친구방을 P0 핵심으로 둔다

친구 연결을 단순한 결과 비교 링크가 아니라 이름과 멤버가 유지되는 방으로 만든다. 방은 매일 새 미션이 열리는 핵심 재방문 장치다.

### 29.3 MVP에서는 활성 친구방을 최대 3개 허용한다

사용자는 관계별로 다른 작은 방을 유지할 수 있다. 공유 대상, 알림, 개인 다이어리 표현은 각 `room_id`를 기준으로 분리하며, 서버 트랜잭션으로 활성 방 수를 최대 3개로 제한한다.

### 29.4 MVP 방 인원은 2~6명이다

1:1만으로 제한하지 않되, 대형 그룹의 운영·알림·모더레이션 문제는 피한다.

### 29.5 MVP에서는 모든 방이 같은 전역 일일 미션을 참조한다

솔로로 시작한 기록과 친구방 기록을 하나로 유지하고, 낮에 방에 참여해도 미션 충돌이 생기지 않게 한다. 데이터 모델은 향후 방별 미션을 지원한다.

### 29.6 친구 사진을 내 다이어리에 넣지 않는다

같이 본 하루와 내 하루를 구분해야 장기적으로 소유권과 프라이버시가 명확해진다.

### 29.7 색상 검증을 MVP 차단 조건으로 두지 않는다

제품의 핵심은 관찰과 기록이지 부정행위 판정이 아니다. 오판으로 기록을 막는 비용이 더 크다.

### 29.8 공개 소셜을 먼저 만들지 않는다

초기 가치는 소수 친구와의 비공개 공동 미션과 개인 아카이브에서 검증한다.

### 29.9 룰렛과 방 미션은 서버가 정한다

멤버마다 결과가 달라지는 문제, 재설치로 재추첨하는 문제, 클라이언트 조작 문제를 방지한다.

## 30. 추후 검증할 질문

아래 질문은 MVP 데이터와 사용자 인터뷰로 검증하되, 구현을 막는 미확정 사항으로 취급하지 않는다.

- 6장 완성 기준이 부담 없이 성취감을 주는가?
- 2명, 3~4명, 5~6명 중 어떤 방 크기가 가장 오래 유지되는가?
- 사용자들은 활성 친구방 최대 3개 제한을 답답하게 느끼는가?
- 방 사진을 실시간 공개하는 것이 좋은가, 특정 시점 공개가 더 기대감을 주는가?
- 새 멤버에게 가입일 이전 방 기록을 어느 범위까지 보여줘야 하는가?
- 방을 나갈 때 과거에 공유한 본인 기록을 유지/삭제 선택하게 해야 하는가?
- 방마다 다른 미션이나 방장 커스텀 미션 수요가 큰가?
- 사용자들이 사진별 캡션을 실제로 쓰는가?
- 월 달력과 방 날짜 기록 중 어떤 화면을 더 자주 재방문하는가?
- 갤러리 업로드 수요가 큰가?
- 색상 일치도 점수가 기록 재미를 높이는가, 평가받는 느낌을 만드는가?

## 31. 기술 참고 자료

구현 시 최신 공식 문서를 우선한다.

- Expo: https://docs.expo.dev/
- Expo Router: https://docs.expo.dev/router/introduction/
- Supabase Expo React Native: https://supabase.com/docs/guides/getting-started/quickstarts/expo-react-native
- Supabase Password Auth: https://supabase.com/docs/guides/auth/passwords
- Supabase RLS: https://supabase.com/docs/guides/database/postgres/row-level-security
- Google Stitch: https://stitch.withgoogle.com/
